{
  "name": "welsh-sm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Predicate over (Lexeme, Environment): does this token exhibit Welsh soft mutation?",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
